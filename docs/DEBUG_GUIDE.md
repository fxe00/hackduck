# HackDuck 调试指南

## 🔧 修复Service Worker问题

### 步骤1：重新加载扩展
1. 访问 `chrome://extensions/`
2. 找到HackDuck扩展
3. 点击"重新加载"按钮（🔄图标）
4. 确认Service Worker状态显示为"有效"

### 步骤2：检查控制台错误
1. 在扩展管理页面，点击HackDuck扩展的"检查视图：Service Worker"
2. 查看控制台是否有错误信息
3. 如果有错误，请截图或复制错误信息

### 步骤3：测试请求拦截

#### 方法1：使用测试页面
1. 访问 https://httpbin.org
2. 按F12打开开发者工具
3. 点击"HackDuck"标签页
4. 开启"拦截请求"开关
5. 在控制台执行：
   ```javascript
   fetch('https://httpbin.org/get')
   ```

#### 方法2：使用简单测试
1. 访问任意网页（如 https://www.google.com）
2. 按F12打开开发者工具
3. 点击"HackDuck"标签页
4. 开启"拦截请求"开关
5. 刷新页面（F5）

### 步骤4：检查网络请求
1. 在开发者工具的"Network"标签页中
2. 确认页面确实发送了HTTP请求
3. 查看请求是否被正常发送

## 🐛 常见问题排查

### 问题1：Service Worker无效
**原因**：TypeScript编译错误
**解决**：重新构建项目，确保没有编译错误

### 问题2：没有捕获到请求
**可能原因**：
1. 拦截开关未开启
2. 页面没有发送HTTP请求
3. 扩展权限不足
4. 消息传递机制有问题

**排查步骤**：
1. 确认拦截开关已开启
2. 检查控制台是否有错误
3. 尝试不同的测试页面
4. 检查扩展权限

### 问题3：请求显示不完整
**原因**：这是正常的，因为：
- 某些请求可能没有请求体
- 某些请求头可能被浏览器过滤
- 这是浏览器安全限制

## 🔍 调试技巧

### 1. 检查扩展状态
```javascript
// 在控制台中检查扩展是否正常工作
chrome.runtime.getManifest()
```

### 2. 检查权限
```javascript
// 检查扩展权限
chrome.permissions.getAll()
```

### 3. 手动触发请求
```javascript
// 在控制台中手动发送请求
fetch('https://httpbin.org/get')
  .then(response => response.json())
  .then(data => console.log(data))
```

### 4. 检查消息传递
在HackDuck面板的控制台中查看是否有日志输出：
- "HackDuck content script loaded"
- "HackDuck injected script loaded"
- "Request captured from content script"

## 📝 测试用例

### 基础测试
```javascript
// 测试fetch请求
fetch('https://httpbin.org/get')

// 测试POST请求
fetch('https://httpbin.org/post', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ test: 'data' })
})

// 测试XMLHttpRequest
const xhr = new XMLHttpRequest()
xhr.open('GET', 'https://httpbin.org/get')
xhr.send()
```

### 复杂测试
```javascript
// 测试带自定义请求头的请求
fetch('https://httpbin.org/headers', {
  headers: {
    'X-Custom-Header': 'test-value',
    'Authorization': 'Bearer token123'
  }
})
```

## ✅ 成功标准

测试成功的标准：
- [ ] Service Worker状态显示为"有效"
- [ ] HackDuck面板正常显示
- [ ] 拦截开关可以正常切换
- [ ] 能够捕获到HTTP请求
- [ ] 请求列表正确显示
- [ ] 可以编辑和发送请求

## 🆘 如果仍然有问题

请提供以下信息：
1. 浏览器版本
2. 扩展版本
3. Service Worker状态
4. 控制台错误信息
5. 测试页面URL
6. 复现步骤
